{{ user_path }}/shared/log/*log {
  # 指定したパーミッション、ユーザ、グループでローテーションを行う
  create 0664 {{ user_name }} {{ user_name }}

  # ログをコピーした後に、既存のログのサイズを0にする
  # ログファイルを閉じないプログラム(Rails含む)は、
  # この指定をしないとずっと同じログファイルに書き込みが行われてしまう
  copytruncate
}
